/* Declare three variables to store a student's name, age, and pass/fail status using
the correct data type for each, then print all three to the console. */
let name = "Rahat";
let age = 25;
let isPass = true;
console.log("Name: " + name + " Age: " + age + " isPass: " + isPass);
