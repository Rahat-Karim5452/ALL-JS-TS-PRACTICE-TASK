// 2. Using the logical AND operator, check whether a person is eligible to vote by
// verifying that their age is 18 or above AND they hold a valid ID (true/false).
let age = 18;
let hasId = false;
if (age >= 18 && hasId == true) {
  console.log("eligible for vote");
} else {
  console.log("Not eligible");
}
