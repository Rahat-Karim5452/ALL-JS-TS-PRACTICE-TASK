// 1. Create an array of 5 fruit names. Add a new fruit to the end using push(), then
// remove the last fruit using pop(). Print the array after each step.
let arr = ["Apple", "Banana", "Mango", "Orange", "Grapes"];
arr.push("Papaya");
console.log(arr);
arr.pop();
console.log(arr);
