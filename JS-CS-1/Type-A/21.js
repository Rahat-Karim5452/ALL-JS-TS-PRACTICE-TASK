// 2. Given an array of numbers, use a for loop to print each element along with its index.
let numbers = [19, 12, 31, 32, 95, 23, 56, 87, 324];
for (let i = 0; i < numbers.length; i++) {
  console.log(i, numbers[i]);
}
