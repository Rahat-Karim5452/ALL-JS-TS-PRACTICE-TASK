// 7. Given an array of 7 numbers, use slice() to copy out and print only the middle three
// elements, then confirm the original array is unchanged.
let numbers = [3, 4, 5, 6, 7, 8, 9];
console.log(numbers.slice(2, 5));
console.log(numbers);
