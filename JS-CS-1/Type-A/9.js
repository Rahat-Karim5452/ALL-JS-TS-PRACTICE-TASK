// 1. Write a program that checks a given number and prints whether it is Positive,
// Negative, or Zero.
let num = -1;
if (num === 0) {
  console.log("Zero");
} else if (num < 0) {
  console.log("Negative");
} else {
  console.log("Positive");
}
