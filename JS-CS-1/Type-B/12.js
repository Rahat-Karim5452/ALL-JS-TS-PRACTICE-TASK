// 4. Check whether a person can apply for a driving license by verifying both age (18+)
// and having a valid ID (a boolean variable), using the logical AND operator.
let age = 18;
let hasValidID = false;
if (age >= 18 && hasValidID == true) {
  console.log("Can apply");
} else {
  console.log("Can't");
}
