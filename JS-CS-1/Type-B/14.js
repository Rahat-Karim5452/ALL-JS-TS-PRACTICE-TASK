// 6. Using the logical NOT operator, write a program that checks if a user is NOT logged
// in (isLoggedIn = false) and prints an appropriate message.
let isLoggedIn = false;
if (!isLoggedIn) {
  console.log("Not Logged In");
} else {
  console.log("Looged In");
}
