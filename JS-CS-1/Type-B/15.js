// 7. Using the logical NOT operator, write a program that checks a Boolean variable
// isLightOn and prints 'Switch is OFF' when the light is not on.
let isLightOn = true;
if (!isLightOn) {
  console.log("Switch is OFF");
} else {
  console.log("Switch is ON");
}
