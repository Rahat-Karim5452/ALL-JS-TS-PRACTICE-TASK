// 1. Create an array of 5 numbers. Use unshift() to add a new number to the beginning,
// then use shift() to remove the first number. Print the array after each step.
let arr = [4, 5, 6, 7, 3, 2];
arr.unshift(99);
console.log(arr);
arr.shift();
console.log(arr);
