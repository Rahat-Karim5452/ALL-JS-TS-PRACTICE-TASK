//TODO: 1. Write a function that takes a name as a parameter and returns a greeting message, then print the returned message.
function greet(name) {
  return "Hello " + name;
}
console.log(greet("Rahat"));
