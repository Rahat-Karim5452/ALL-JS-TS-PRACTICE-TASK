//TODO: 1. Given a string, print it in both uppercase and lowercase using the appropriate string methods.
let str = "soMthinG is BetteR than Nothing";
let upper = str.toUpperCase();
console.log(upper);
let lower = str.toLowerCase();
console.log(lower);
