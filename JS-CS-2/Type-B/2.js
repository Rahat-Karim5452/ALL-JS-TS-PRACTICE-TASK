//TODO:2. Given a string with leading and trailing spaces, print its length before trimming and after trimming to show the
//TODO: - difference.
let str = " somthing is better than nothing ";
console.log(str.length);
str = str.trim();
console.log(str);
console.log(str.length);
