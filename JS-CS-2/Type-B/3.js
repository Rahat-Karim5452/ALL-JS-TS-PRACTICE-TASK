//TODO:3. Use slice() to extract and print the first three characters and the last three characters of a given string.
let str = "somthing is better than nothing";

let firstThree = str.slice(0, 3);
console.log(firstThree);

let lastThree = str.slice(-3);
console.log(lastThree);
