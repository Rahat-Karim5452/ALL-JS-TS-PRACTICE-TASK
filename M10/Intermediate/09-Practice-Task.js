// 9. Given an array of numbers, calculate and log the total sum, using a loop.
let arr = [50, 10, 5, 100, 25];
let sum = 0;
for (let i = 0; i < arr.length; i++) {
  sum += arr[i];
}
console.log(sum);
