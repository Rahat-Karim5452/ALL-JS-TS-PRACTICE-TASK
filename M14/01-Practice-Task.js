/* Below are three small broken snippets. For each, identify the error type (Syntax / Runtime / Logic),
then fix it.

function sayHi(name) {
console.log("Hi, " + name / find the bug
}
sayHi("Mira");

i/p: sayHi("Mira") => o/p: Hi, Mira
*/
function sayHi(name) {
  console.log("Hi, " + name);
}
sayHi("Mira");
