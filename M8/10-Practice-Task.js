/* Build a simple login system — print "Login successful" only if both username and
password are correct. */
let username = "rahat";
let password = 12345;
if (username === "rahat" && password === 12345) {
  console.log("Login Successful");
} else {
  console.log("Try again");
}
